meta {
  name: 02_Get Users
  type: http
  seq: 2
}

get {
  url: http://localhost:{{PORT}}/user
  body: none
  auth: inherit
}

script:pre-request {
  const token = bru.getVar("token");
  req.setHeader("Authorization", `Bearer ${token}`);
}

settings {
  encodeUrl: true
  timeout: 0
}
