meta {
  name: 00_Create the test user
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/user
  body: json
  auth: inherit
}

body:json {
  {
    "name": "TestFromGateway",
    "isAdmin": false
  }
}

script:post-response {
  console.log(res.body);
  bru.setVar("user_id", res.body.id);
  bru.setVar("user_name", res.body.name);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
