meta {
  name: 01_Connect User
  type: http
  seq: 2
}

post {
  url: http://localhost:{{PORT}}/user/connect
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{user_name}}"
  }
}

script:pre-request {
  const user_name = bru.getVar("user_name")
}

script:post-response {
  bru.setVar("token", res.body);
}

settings {
  encodeUrl: true
  timeout: 0
}
